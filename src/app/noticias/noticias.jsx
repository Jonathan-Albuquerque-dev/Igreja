import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function App() {
  return (
    <View>
      <SafeAreaView>
        <Text>Pagina noticias</Text>
      </SafeAreaView>
    </View>
  );
}
